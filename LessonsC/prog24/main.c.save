#include <stdio.h>
/*Решето Эратосфена (Тимофей Хирьянов)*/
#define N 20

int main()
{
    int sieve[N] = {0};
    
    for(int i = 2; i*i < N; i++)
    if(sieve[])
    for(int k = )
    
    for(int i = 0; i < N; i++)
    printf("%3d", i);
    printf("\n");
    for(int i = 0; i < N; i++)
    printf("%3d", sieve[i]);

    return 0;
}
