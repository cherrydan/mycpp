#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x = 2048;
    int res = 1;
    printf("Число целиком: %d\n", x);

    //получаем разряды числа
    while(x != 0){
    res*= x % 10;
    x /= 10;
    }
    printf("Перемноженые разряды: %s\n", sprintf(res);
    return 0;
}
